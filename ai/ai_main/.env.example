# 자격증명 (필수, 기본값 없음)
GEMINI_API_KEY=
INTERNAL_TOKEN=

# Spring 내부 API
SPRING_BASE_URL=http://spring-api:8080

# S3
S3_BUCKET=
AWS_REGION=ap-northeast-2

# OpenSearch (검색 — FastAPI 전담)
# docker compose 로 띄우면 호스트는 컨테이너 이름(opensearch)이다.
# 보안 플러그인을 켜므로 HTTPS + 인증을 쓴다. 인증서는 자체 서명이라 검증은 끈다.
# 비밀번호는 대문자·소문자·숫자·특수문자 포함 8자 이상이어야 한다(2.12+ 요구사항).
# OPENSEARCH_INITIAL_ADMIN_PASSWORD 와 OPENSEARCH_PASSWORD 는 같은 값을 넣는다.
OPENSEARCH_HOST=opensearch
OPENSEARCH_PORT=9200
OPENSEARCH_USE_SSL=true
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=
OPENSEARCH_INITIAL_ADMIN_PASSWORD=
OPENSEARCH_INDEX=screenshot_kb

# 모델
LLM_MODEL_NAME=gemini-3.5-flash
VISION_MODEL_NAME=gemini-3.5-flash
EMBEDDING_MODEL_NAME=gemini-embedding-2

# 분류 파라미터
SIMILARITY_THRESHOLD=0.80
DEFAULT_MAX_TAGS=10
HTTP_TIMEOUT=30

# 대량 처리 안정성
# 값 뒤에 주석을 붙이면 docker compose 가 주석까지 값으로 읽어 숫자 변환에 실패한다.
# MAX_CONCURRENT_STAGES: 동시에 처리할 단계 수 (Gemini 폭주/429 방지)
# GEMINI_MAX_ATTEMPTS: 429 재시도 횟수
# GEMINI_BACKOFF_BASE: 재시도 백오프 기준(초), 지수 증가
# GEMINI_BACKOFF_MAX: 백오프 상한(초)
MAX_CONCURRENT_STAGES=4
GEMINI_MAX_ATTEMPTS=5
GEMINI_BACKOFF_BASE=1.0
GEMINI_BACKOFF_MAX=30
